import React from 'react';

function ThreeScene({order, company, years, title}) {
    return (
        <div className={"expContainer"} style={{animationDelay: order*0.10 +"s"}}>
           
        </div>
    );
}

export default ThreeScene;